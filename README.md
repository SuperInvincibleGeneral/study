# claude-template

Claude と作業する新規プロジェクト用のテンプレートリポジトリ。

## 使い方

1. 「Use this template」で新規プロジェクトを開始する
2. `CLAUDE.md` のプロジェクト名（`{プロジェクト名}`）を書き換える
3. Python プロジェクトなら common-lib の `tools/setup_project.ps1` を実行する
