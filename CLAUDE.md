# {プロジェクト名}

## 共通ルール（Cycle）

以下の順で Cycle を探し、最初に見つかった場所の `CLAUDE.md` を読み込んで従うこと：

1. `~/Cycle` または `../Cycle`（ローカルPC・Webセッションでリポジトリ追加済みの場合）
2. 無ければ GitHub の `SuperInvincibleGeneral/Cycle` を取得して読む（clone または GitHub 連携）
3. それも不可なら「Cycle 未到達のため改善サイクルなしで動作中」と冒頭で宣言して続行する

環境判定と環境別の振る舞い → Cycle の `rules/environment_rules.md`

## このリポジトリ固有のルール

`rules_local/` を参照（無ければ固有ルールなし）。

## このリポジトリについて

（プロジェクト固有の説明をここに書く）
